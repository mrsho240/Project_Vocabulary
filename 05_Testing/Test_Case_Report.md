# Test Case Report
**Project Name:** Vocabulary Quiz Generator
**Tester:** [mrsho240]
**Date:** [20/11/2025]

---

## บทสรุปผลการทดสอบ (Test Summary)
การทดสอบครอบคลุมฟังก์ชันการทำงานหลัก (Functional Testing) ของระบบ ได้แก่ การนำเข้าข้อมูลจากไฟล์ JSON, ระบบการสุ่มคำถาม, การตรวจสอบความถูกต้องของคำตอบ, การคำนวณคะแนน และการแสดงผลส่วนติดต่อผู้ใช้ (UI)

* **จำนวน Test Case ทั้งหมด:** 12 ข้อ
* **สถานะ:** ผ่าน (Pass) ทั้งหมด

---

## รายละเอียดการทดสอบ (Test Cases)

| ID | Test Scenario (กรณีทดสอบ) | Test Steps (ขั้นตอนการทดสอบ) | Expected Result (ผลลัพธ์ที่คาดหวัง) | Actual Result | Status |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC-01** | **การเปิดโปรแกรม (Application Launch)** | 1. รันไฟล์โปรแกรม (Vocabulary Quiz Generator.jar/Main Class)<br>2. รอจนกว่าโปรแกรมจะโหลดเสร็จ | หน้าต่างโปรแกรมแสดงขึ้นมาพร้อมหน้า "Main Menu" และปุ่ม Start, Vocabulary, About Me ครบถ้วน | ตามที่คาดหวัง | **Pass** |
| **TC-02** | **การนำทางไปหน้าเลือกความยาก** | 1. ที่หน้า Main Menu คลิกปุ่ม "Start" | โปรแกรมเปลี่ยนไปยังหน้า "Select Quiz Difficulty" | ตามที่คาดหวัง | **Pass** |
| **TC-03** | **การโหลดไฟล์ข้อมูล (Data Loading)** | 1. ตรวจสอบว่ามีไฟล์ `vocab_data.json` ในโฟลเดอร์<br>2. กดเลือกความยาก "Beginner" เพื่อเริ่มเกม | โปรแกรมสามารถอ่านไฟล์ได้ และแสดงคำถามแรกโดยไม่เกิด Error (ข้อความคำถามไม่เป็นค่าว่าง) | ตามที่คาดหวัง | **Pass** |
| **TC-04** | **การแสดงผลหน้าทำข้อสอบ (Quiz Interface)** | 1. เข้าสู่หน้าทำข้อสอบ<br>2. สังเกตองค์ประกอบหน้าจอ | แสดง Category, Difficulty, โจทย์คำถาม, และตัวเลือก 4 ข้อ ได้ถูกต้องตาม Design | ตามที่คาดหวัง | **Pass** |
| **TC-05** | **การทำงานของปุ่มย้อนกลับ (Navigation Back)** | 1. ที่หน้า "Select Quiz Difficulty"<br>2. กดปุ่มลูกศรย้อนกลับ (Back) | โปรแกรมกลับมายังหน้า Main Menu | ตามที่คาดหวัง | **Pass** |
| **TC-06** | **การเปลี่ยนข้อ (State Management)** | 1. ตอบคำถามข้อที่ 1 เสร็จสิ้น<br>2. กดถัดไป หรือรอระบบเปลี่ยนข้ออัตโนมัติ | 1. โจทย์คำถามเปลี่ยนเป็นข้อใหม่ (ไม่ซ้ำข้อเดิม)<br>2. ตัวเลขข้อปัจจุบันเปลี่ยนจาก 1/10 เป็น 2/10 | ตามที่คาดหวัง | **Pass** |
| **TC-07** | **การสุ่มตัวเลือก (Randomization)** | 1. สังเกตตำแหน่งของคำตอบที่ถูกต้องในแต่ละข้อ | ตำแหน่งของคำตอบที่ถูกต้องต้องไม่ซ้ำที่เดิมตลอด (เช่น ไม่ใช่ตอบ A ตลอดทุกข้อ) | ตามที่คาดหวัง | **Pass** |
| **TC-08** | **การจบแบบทดสอบ (Quiz Completion)** | 1. ทำแบบทดสอบครบทั้ง 10 ข้อ | โปรแกรมเปลี่ยนไปยังหน้า "Score Summary" (หน้าสรุปผล) | ตามที่คาดหวัง | **Pass** |
| **TC-09** | **การคำนวณคะแนนรวม (Scoring Logic)** | 1. นับจำนวนข้อที่ตอบถูกจริงระหว่างทำสอบ (เช่น ถูก 8 ข้อ)<br>2. เปรียบเทียบกับหน้าสรุปผล | หน้าสรุปผลต้องแสดงคะแนน "8/10" และเปอร์เซ็นต์ "80%" ได้ถูกต้องตรงกัน | ตามที่คาดหวัง | **Pass** |
| **TC-10** | **การเริ่มเล่นใหม่ (Replay Feature)** | 1. ที่หน้าสรุปผล กดปุ่ม "Play Again"<br>2. เริ่มทำข้อสอบใหม่อีกครั้ง | 1. คะแนนกลับไปเริ่มที่ 0<br>2. ตัวนับข้อกลับไปที่ 1/10<br>3. คำถามชุดใหม่ถูกสุ่มขึ้นมา | ตามที่คาดหวัง | **Pass** |

---

## หมายเหตุ (Notes)
* **Test Environment:** Windows 10, Java Development Kit (JDK) 21
* **Data Source:** ทดสอบโดยใช้ไฟล์ `vocab_data.json` ตัวอย่างที่มีข้อมูลคำศัพท์ 50 คำ